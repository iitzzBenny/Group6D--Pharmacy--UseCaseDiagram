package GeneralPractitioner;
import Drugs.*;

public class GPServer extends Appointment {

    public GPServer(String firstName, String lastName, int age, String address, String customerID) {
        super(firstName, lastName, age, address, customerID);
    }
}
